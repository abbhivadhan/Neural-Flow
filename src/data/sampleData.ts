// Imports disabled due to type issues
// import { Task, TaskStatus, WorkType, ComplexityLevel, EnvironmentType, CollaborationLevel } from '../types/task';
// import { Project, ProjectStatus, ProjectRole, ProjectPermission, ProjectVisibility, RiskType, RecommendationType } from '../types/project';
// import { Priority } from '../types/common';

// Sample Tasks - DISABLED DUE TO TYPE ISSUES - USING EMPTY ARRAY
export const sampleTasks: any[] = [];

// Sample Projects - DISABLED DUE TO TYPE ISSUES - USING EMPTY ARRAY
export const sampleProjects: any[] = [];